apiVersion: v1
kind: Service
metadata:
  name: aigen-service
  labels:
    app: aigen
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: aigen
